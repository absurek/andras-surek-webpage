---
import '@fontsource/oswald';
import '@fontsource/source-serif-pro';
---

<style is:inline is:global>
  :root {
    --aw-font-sans: 'Source Serif Pro';
    --aw-font-serif: var(--aw-font-sans);
    --aw-font-heading: 'Oswald';
  }

  .dark {
    --aw-font-sans: 'Source Serif Pro';
    --aw-font-serif: var(--aw-font-sans);
    --aw-font-heading: 'Oswald';
  }
</style>
